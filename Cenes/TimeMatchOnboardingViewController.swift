//
//  TimeMatchOnboardingViewController.swift
//  Deploy
//
//  Created by Cenes_Dev on 15/07/2019.
//  Copyright © 2019 Cenes Pvt Ltd. All rights reserved.
//

import UIKit

class TimeMatchOnboardingViewController: UIViewController {

   // @IBOutlet weak var predictiveBubblesView: UIView!
    
   // @IBOutlet weak var predictiveGreenbubble: UIView!
   // @IBOutlet weak var predictiveGreyBuubl: UIView!
    
   // @IBOutlet weak var predictiveLimeBubble: UIView!
    
   // @IBOutlet weak var predictiveRedBubble: UIView!
    
   // @IBOutlet weak var predictivePinkBubble: UIView!
    
    var itemIndex: Int = 1;


    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
        /*predictiveGreenbubble.layer.borderColor = UIColor.white.cgColor;
        predictiveGreenbubble.layer.borderWidth = 5;
        predictiveGreenbubble.layer.cornerRadius = predictiveGreenbubble.frame.height/2
        
        predictiveGreyBuubl.layer.borderColor = UIColor.white.cgColor;
        predictiveGreyBuubl.layer.borderWidth = 5;
        predictiveGreyBuubl.layer.cornerRadius = predictiveGreyBuubl.frame.height/2
        
        
        predictiveLimeBubble.layer.borderColor = UIColor.white.cgColor;
        predictiveLimeBubble.layer.borderWidth = 5;
        predictiveLimeBubble.layer.cornerRadius = predictiveLimeBubble.frame.height/2
        
        
        
        predictiveRedBubble.layer.borderColor = UIColor.white.cgColor;
        predictiveRedBubble.layer.borderWidth = 5;
        predictiveRedBubble.layer.cornerRadius = predictiveRedBubble.frame.height/2
        
        
        predictivePinkBubble.layer.borderColor = UIColor.white.cgColor;
        predictivePinkBubble.layer.borderWidth = 5;
        predictivePinkBubble.layer.cornerRadius = predictivePinkBubble.frame.height/2*/
    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
